{
  "name": "resellerhub-bd",
  "version": "1.0.0",
  "private": true,
  "description": "ResellerHub BD - static frontend + shared data store (JSON by default, MySQL optional)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "optionalDependencies": {
    "mysql2": "^3.11.0"
  }
}
